<body>
    <main>
        <h3 class="product__price"><%= card.description %></h3>
        <% if(comments.length > 0){ %>
            <% for(var i = comments.length -1; i >= 0; i--) { %>
                <p><%= usernames[i] %></p>
                <p><%= comments[i].text %></p>
            <% } %>
        <% } else { %>
            <h1>bar</h1>
        <% } %>
    </main>
</body>
